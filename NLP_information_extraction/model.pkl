# Import libraries
import spacy

!pip install pdfminer.six  #pdfminer.six is a tool used for extracting text from PDF files.

from pdfminer.high_level import extract_text #the "extract_text" function is used to extract the text from a PDF file and then pass it to a spaCy object for further processing.

# Load spaCy model
nlp = spacy.load("en_core_web_sm")

# Extract text from pdf file
text = extract_text(pdf_file)

# Create a spaCy object from the text
doc = nlp(text)

# Create an empty dictionary to store the entities and their counts
entities = {}

# Loop through the entities in the doc object
for ent in doc.ents:
  # If the entity is already in the dictionary, increment its count by one
  if ent.text in entities:
    entities[ent.text] += 1
  # Otherwise, add the entity to the dictionary with a count of one
  else:
    entities[ent.text] = 1

# Loop through the dictionary and print the entity and its count
for entity, count in entities.items():
  print(entity, count)
